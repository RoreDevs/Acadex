// Default attendance radius fallback (meters). The authoritative value lives in the
// `settings` table (`attendance_radius_meters`). Keep this in sync with migration 011.
export const DEFAULT_ATTENDANCE_RADIUS_METERS = 500;
